import { StyleProductsDirective } from './style-products.directive';

describe('StyleProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleProductsDirective();
    expect(directive).toBeTruthy();
  });
});
