import { CriteCardPipe } from './crite-card.pipe';

describe('CriteCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CriteCardPipe();
    expect(pipe).toBeTruthy();
  });
});
